service : {
  balance: () -> (nat);
  balanceOf: (principal) -> (nat) query;
  getPrincipal: () -> (principal);
  getSymbol: () -> (text) query;
  mint10k: () -> (text);
  transfer: (principal, nat) -> (text);
}
